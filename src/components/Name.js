import React from 'react'
import styled from 'styled-components'

const Name = ({name}) => {
    return (
        <div>
            <h2>Species: {name}</h2>
        </div>
    )
}

export default Name
